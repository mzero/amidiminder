#include "client.h"

#include "msg.h"


namespace Client {

  void connectCommand() {
    throw Msg::runtime_error("Connect command still stubbed out.");
  }

  void disconnectCommand() {
    throw Msg::runtime_error("Disconnect command still stubbed out.");
  }

}
